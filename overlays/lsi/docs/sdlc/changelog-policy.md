# Changelog policy

When `version.txt` (or `VERSION`) increases, `CHANGELOG.md` must include `## [X.Y.Z]` for that version.

Use `/lsi:changelog` or `scripts/release/generate_changelog.py` to format entries from conventional commits.
