# Bundle maintainer local install

Gitignored `.cursor/` for **this repo only** — not copied to adopters via `adopt.py`.

**Documentation:** gitignored [MAINTAINER.md](../../MAINTAINER.md) at the bundle root (copy from [MAINTAINER.md.example](../../MAINTAINER.md.example)).

| Source | Installed to |
|--------|----------------|
| `overlays/lsi/agent-stack/commands/*.md` | `.cursor/commands/` |
| `snippets/cursor-rules/commit-pr-conventions.mdc` | `.cursor/rules/` |
| `snippets/maintainer-local/rules/local-*.mdc` | `.cursor/rules/` |

Install and re-sync steps are in **MAINTAINER.md** only.
